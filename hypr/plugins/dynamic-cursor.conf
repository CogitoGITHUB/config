plugin:dynamic-cursors {
    enabled = true
    mode = rotate
    threshold = 3
    rotate {
        length = 10
        offset = 0.0
    }

    shake {
        enabled = true
        nearest = true
        threshold = 5.0
        base = 2.5 
        speed = 1.0 
        influence = 4.0
        limit = 0.0
        timeout = 1000
        ipc = false
    }

    hyprcursor {
        nearest = 1 
        enabled = true
        resolution = -1
        fallback = clientside
    }
}
