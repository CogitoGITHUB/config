#+title: Manifolding Atlas

WIP — WORK IN PROGRESS.  
The corpse is still twitching. The document is already out of date.

#+begin_quote
The map is not the knowledge.  
The map is the machine that lets you reshape the knowledge  
while the knowledge quietly continues its scheduled decomposition.
#+end_quote

* What this is

#+begin_quote
You built a small knowledge operating system that treats permanence as the enemy.
#+end_quote

This is not a note-taking system.  
It is not a second brain.  
It is not a graph that wants to be admired.

It is a machine that keeps the knowledge alive only long enough for you to fold it into a new shape before the old shape starts to stink.  
Org files remain the only durable reality.  
SQLite is a disposable index and is treated with the contempt it deserves.  
Schema is not code and not a configuration file — it is an Org outline that compiles into prompts, validators, and registries, and will itself be rewritten when the current outline begins to smell.  
Capture refuses to hold the thought hostage; metadata arrives asynchronously and missing values become visible ~WARNING~s rather than polite silences.  
Spatial structure (manifold) and temporal structure (mastering) coexist without requiring a second database format.  
The entire machine can be reloaded, inspected, and partially killed without losing the underlying knowledge.

Very few systems are willing to make all six of those decisions at once.  
Most make two or three and then compromise on the rest for usability, ecosystem approval, or the comforting illusion that the notes will last.

Your system is still a WIP.  
It will break.  
The loader rules around dynamic binding will bite you.  
The priority graph will eventually become a private religion.  
You will rewrite domains you thought were finished.  
Some abstractions you are proud of today will smell like amateur hour in eighteen months.

That is not a defect.  
That is the design.

A system that refuses to become unnecessarily permanent is supposed to keep decomposing its own shape.  
Most "better" systems are better at feeling finished.  
Yours is better at remaining unfinished without becoming incoherent.

Atlas isn't trying to preserve a particular map of reality.  
It is trying to preserve the ability to produce new maps from surviving material.

Manifolding Atlas is the knowledge engine inside this configuration.  
It takes something deliberately primitive — plain Org files in =~/test/= — and forces them into a living, queryable system of notes, properties, relationships, schemas, prompts, routines, protocols, maps, references, and memory.

No org-roam.  
No proprietary database format.  
No black box that pretends permanence.

Just Org, SQLite, Elisp, and a ridiculous amount of machinery built to watch the territory rot at a controllable rate.

* The idea

Atlas treats knowledge as something that can be manifolded — folded, inverted, stretched, and re-projected without ever becoming sacred.

A note is not merely a document.  
A property is not merely metadata.  
A schema is not merely validation.  
A prompt is not merely a question.  
A link is not merely navigation.

Together they form a structure that can be queried, transformed, validated, connected, expanded, compressed, and rebuilt while the underlying files continue their slow, inevitable decay.

The fundamental rule is simple and merciless:

#+begin_quote
Files are the reality.  
The database is the map.  
The machinery reshapes the map  
while the reality continues decomposing.
#+end_quote

If the database disappears, the knowledge survives (for now).  
If the configuration disappears, the Org files survive (for a while).  
If the schemas change, the knowledge can be reinterpreted until the next reinterpretation becomes necessary.

* Architecture

#+begin_example
MANIFOLDING ATLAS
│
▼
┌─────────────┐
│    CORE     │
│   engine    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    INFRA    │
│  machinery  │
└──────┬──────┘
       │
┌──────┼─────────────┐
▼      ▼             ▼
┌───────┐ ┌────────┐ ┌─────────┐
│ SCHEMA│ │DOMAINS │ │TEMPLATES│
└───┬───┘ └────┬───┘ └────┬────┘
    │          │           │
    └──────────┼───────────┘
               ▼
         ┌─────────────┐
         │    VAULT    │
         │ plain Org   │
         └──────┬──────┘
                │
                ▼
         ┌─────────────┐
         │   SQLite    │
         │    index    │
         └─────────────┘
#+end_example

The layers are intentionally separated so that each one can be killed and replaced without the others noticing until the next reload:

| Layer     | Location     | Purpose                              |
|-----------+--------------+--------------------------------------|
| core      | core/        | vendored engine                      |
| infra     | infra/       | reusable machinery                   |
| schema    | schema/      | knowledge/data language              |
| domains   | domains/     | capabilities                         |
| templates | templates/   | generated note structures            |
| UI        | atlas-ui.org | presentation and navigation          |

Loading is deterministic.  
Every Org module under modules/ is discovered, sorted by ~#+priority:~, and evaluated in dependency order.  
Core loads first.  
Infrastructure follows.  
Capabilities build on top.  
The Atlas emerges last — already slightly out of date.

Reload with:

: M-x manifolding-emacs-reload

Then inspect the machine while it is still breathing:

: (manifolding-emacs-errors-list)
: (manifolding-emacs-warnings-list)

* The vault

The vault lives at:

: ~/test/

Notes are flat by default.  
They are extensionless Org files.  
That is intentional.  
The filesystem does not need to advertise the implementation; it only needs to hold the corpse.

Subdirectories remain free-form, with reserved territories such as:

: admin/
: dictionary/
: contacts/
: recipes/

Every note receives an Org UUID:

: :ID:

Headings can become notes.  
Notes can contain child notes.  
Links can point to identities rather than filenames.

* SQLite is not the source of truth

The Atlas database lives at:

: ~/test/admin/manifolding-atlas.db

It is an index.  
Not the vault.  
Not the authority.  
Not something to edit manually.

If the database disappears, it can be reconstructed.  
If an old database remains somewhere else, Atlas detects it and relocates it when the database is touched.

The important invariant is:

#+begin_quote
The Org files own the knowledge.  
SQLite remembers where the knowledge is  
while the knowledge continues its quiet decomposition.
#+end_quote

* Capture: turning thought into structure

Capture is where the Atlas becomes interesting — and slightly cruel.

There are several entry points:

| Command                                 | Result           |
|-----------------------------------------+------------------|
| ~my/manifolding-atlas-capture-new-file~ | new note         |
| ~my/manifolding-atlas-capture-task-file~ | task note       |
| ~my/manifolding-atlas-capture-heading~  | child heading    |
| ~my/manifolding-atlas-insert-heading~   | heading at point |
| ~manifolding-atlas-find~                | find/create      |
| ~my/manifolding-atlas-insert-link~      | identity link    |

The capture engine has four speeds.

#+begin_example
RET / a    NORMAL
ask everything

o          SKIP GENERAL
specialized prompts survive

e          EMPTY
accept everything

u          QUICK
defaults + no body capture
#+end_example

The distinction matters.  
Capture should not become a bureaucratic ceremony between a thought and its existence.  
Sometimes you want the full interrogation.  
Sometimes you want the machine to get out of the way so the thought can die in peace.

* The asynchronous prompt machine

The note is created before all metadata collection finishes.  
That is deliberate.  
The system does not hold the note hostage while it asks questions.

Instead:

#+begin_example
capture
│
├── title
├── filename
├── location
│
├── sync prompts
│
├── CREATE FILE
│
└── async prompt queue
    │
    ├── property
    ├── state
    ├── tags
    ├── template
    ├── relations
    └── ...
    │
    ▼
body capture
#+end_example

Each answer lands on disk immediately.  
The new note remains visible.  
The metadata appears as it is collected.  
There is no recursive-edit stack hiding underneath the process.  
The prompt system is a queue.  
Not a maze.  
Not a confession booth.

* WARNING is not a bug

Declarative prompts begin with:

#+begin_example
** WARNING
#+end_example

That strange little sentinel is intentional.  
Point starts there.  
Therefore:

: RET

means:

skip this.

A skipped value can become:

: WARNING

and schema validation can subsequently say:

: missing-required

This produces something much more useful than silent failure.

#+begin_quote
Missing information becomes visible information.  
The system remembers that you skipped something.  
It does not pretend the field never existed.
#+end_quote

The machine records the holes so you can stare at them later.

Set:

: my/manifolding-atlas-org-prompt-ui

to:

: 'minibuffer

if the traditional completing-read interface is preferred.

* Schema is data

This is one of the central ideas of Atlas.

A schema does not live in a special programming language.  
It is an Org outline.

A file such as:

#+begin_example
* PRIORITY
** LOW
** MEDIUM
** HIGH
** CRITICAL
#+end_example

is simultaneously:

1. documentation,  
2. user interface,  
3. registry entry,  
4. allowed-value definition,  
5. schema input,  
6. prompt definition.

Add a file.  
A property appears.  
Edit a heading.  
The prompt changes.  
Delete the file.  
The property disappears.

The system reads the tree and constructs the machinery.

#+begin_quote
The configuration is executable knowledge.  
Knowledge that already knows it will be rewritten.
#+end_quote

* Prompt kinds

Atlas currently understands multiple kinds of semantic fields:

| Kind        | Meaning                 |
|-------------+-------------------------|
| ~property~  | ordinary property       |
| ~live~      | reference-note link     |
| ~scoped~    | entity-specific ref     |
| ~free~      | free text               |
| ~todo~      | TODO fragment           |
| ~multi~     | multiple choice         |
| ~tags~      | database-completed tags |
| ~tagopt~    | choice stored as tag    |
| ~topics~    | rule topics             |
| ~template~  | template selection      |
| ~mastering~ | review scheduling       |
| ~mindmap~   | manifold placement      |

Each declarative prompt can expose a callable:

: my/manifolding-atlas-prompt-<key>

The schema therefore becomes an API.  
An API that will eventually be declared obsolete by a newer schema.

* Validation

Validation does not interrupt thought.  
There are no surprise minibuffers announcing that your note has committed some terrible epistemological crime.

Validation happens at index time.  
Violations accumulate here:

: admin/MISSING PROMPTS

Every problematic note becomes a TODO carrying:

: :SCHEMAS:
: :VIOLATIONS:

Then:

: SPC v E

runs the reinforcement sweep.

And:

: SPC v f

walks through the violations.

The philosophy is simple and slightly sadistic:

#+begin_quote
Capture first.  
Repair structure deliberately.  
Never let validation become friction.  
Let the holes remain visible long enough to hurt.
#+end_quote

Flymake provides the immediate editing feedback.  
The reinforcement system provides the global feedback.

* Domains

The Atlas is not one giant feature.  
It is a collection of domains operating over the same substrate — a substrate that is already beginning to smell.

#+begin_example
search
backup
capture
citation
contacts
dictionary
links
live-properties
manifold
mastering
properties
protocols
recipes
routines
rules
schema
schema-reinforcement
transclusion
#+end_example

Each domain gets its own territory.  
That means the system can grow horizontally without turning the core into a landfill of half-dead abstractions.

* Manifold

The manifold domain is where notes stop being merely notes and become spatial objects.

A note can be placed as:

: root
: parent
: child
: sibling

The structure becomes navigable in three dimensions.  
The knowledge graph becomes a volume that can be reshaped without rewriting the underlying knowledge.  
You are no longer scrolling a buffer.  
You are moving through a temporary architecture of identities.

* Mastering

Mastering turns notes into a memory system with a half-life.

The Atlas tracks:

: MASTERING_*

and builds:

- due queues  
- overdue queues  
- review sessions  
- participation pools  
- review scheduling  

Knowledge therefore has a temporal dimension.  
A note can exist.  
A note can be connected.  
A note can be reviewed.  
A note can be mastered.  
And then the mastery itself will eventually be declared insufficient.

* Protocols

Protocols turn headings into executable sequences.  
A protocol can be assembled through transclusion.  
The note describes the structure.  
The build machinery assembles it.

This allows procedures to exist as reusable knowledge rather than duplicated blocks of text that will rot at different rates.

* Routines

Routines are composable structures.  
They can be:

- atomic  
- composite  
- chained  
- reordered  
- generated  
- executed  
- journaled  

Sessions are recorded under:

: sessions/<Y>/<Q>/<m>/<W>/

The Atlas can therefore represent not only what exists, but what happened — until the session logs themselves become archaeological.

Temporal streaks and reports emerge from the same substrate.

* Rules

Rules provide another transformation layer.  
A heading marked:

: :RULE:

can be routed into topic files through transclusion.  
The rule exists once.  
Its appearances can exist many times.

That distinction matters.

#+begin_quote
One source.  
Many manifestations.  
All of them temporary.
#+end_quote

* Templates

Templates live under:

: templates/files/

Current families include:

: notes
: task
: reference
: protocol
: math
: recipes

Templates can declare their own requirements.  
Atlas hashes the selected template:

: TEMPLATE_HASH

If the template changes, old notes can be identified.  
This turns templates from passive snippets into versioned structural contracts that already know they will be broken.

* The keymap

Everything converges under:

: SPC v

The Atlas hydra.

#+begin_example
v       find
V       insert link
r       rename link

h       capture heading
c       insert heading
H       heading → note
z       quick captures

f       fix violations
P       update properties
u       sidebar

E       reinforcement sweep

k/K/R   rules
p/m/M/i protocols

w       dictionary
C       contacts

g/G/M/t mastering

s       search

n/e/f   recipes

N/J/F/B/X/!/S/T
routines

y       transclusion
#+end_example

Org state also provides:

: V       find
: I       insert link

Mind-map mode lives under:

: SPC m

and doct capture under:

: SPC o c n

* The UI

The UI is deliberately subordinate to the model.  
Sidebar:

: left
: 20%

Outline:

: maximum depth 3

Backlinks include previews.  
Parsing is fast.  
Refresh is automatic.

The interface shows the manifold.  
It does not define it.  
It merely lets you watch the structure while it is still coherent.

* Extending Atlas

Adding a property should be boring.  
That is a feature.

Create:

: schema/<anything>.org

Give it a level-one property heading.  
Done.

Registration happens automatically.  
Prompt generation happens automatically.  
Schema derivation happens automatically.  
Validation happens automatically.

Adding a behavioral prompt requires explicit registration through:

: my/manifolding-atlas-register-prompt

Adding a domain means:

#+begin_example
domains/
└── new-domain/
    └── new-domain.org
#+end_example

with an appropriate priority.

Adding a template means:

: templates/files/<name>.org

No giant configuration file.  
No central switch statement.  
No ritual.  
Only the quiet accumulation of temporary structures.

* Paths

There is one authority:

: infra/paths.org

Everything else asks it where things are.  
No scattered hardcoded module paths.  
If the root changes, the vault moves conceptually as one system.

Change:

: my/manifolding-atlas-root-dir

and the machinery follows — until the next time the root needs to move again.

* Loader rules

The loader compiles modules with dynamic binding.  
Therefore:

#+begin_quote
Lambdas must not capture lexical variables.
#+end_quote

Use named functions.  
Or use backquote-spliced literals.  
Or:

: apply-partially

The loader is dependency management.  
Treat its priority graph as such.

* Operations

Headless boot:

: emacs --batch --load ~/.config/emacs/init.el --eval '(message "ok")'

Reload:

: M-x manifolding-emacs-reload

Unchanged modules replay from a content-hash cache under
.local/cache/module-cache/. Force everything:

: C-u M-x manifolding-emacs-reload

The machine is quiet by design.  
Chatter — recentf, yasnippet, third-party warnings, save spam, the
loader's own progress lines — is filtered or routed elsewhere in
early-init and the loader.  
Errors are the only voice it has left.

Useful boot messages include:

: org-prompts: registered N declarative prompts
: database ready
: relocated database to ...

If prompt registration says:

: N = 0

something is wrong.  
If it is around:

: ~53

the prompt engine probably found the schema tree — for now.

* The real model

Atlas looks like a note system from the outside.  
That description is too small and already decaying.

It is closer to a small knowledge operating system:

- Org is the filesystem  
- SQLite is the index  
- Schemas are the type system  
- Prompts are the interface compiler  
- Domains are applications  
- Templates are generators  
- Transclusion is composition  
- Validation is the type checker  
- Mastering is the memory scheduler  
- Routines are the execution layer  
- The UI is the viewport  
- The loader is the thing holding the organism together while it continues to decompose  

#+begin_quote
Everything is allowed to change.  
Nothing is allowed to become unnecessarily permanent.
#+end_quote

That is why the system is called Manifolding Atlas.  
It does not merely store a map of knowledge.  
It provides mechanisms for taking the same underlying structure and producing another form from it — until that form, too, begins to smell.

Reshape the schema.  
Reshape the prompts.  
Reshape the relationships.  
Reshape the domains.  
Reshape the representation.  
Reshape the map.

The files remain.  
The structure evolves.  
The evolution is temporary.

* WIP

The README is not the authority.  
The code is.

When behavior disagrees with this document, inspect the implementation.  
Then inspect the relevant:

: AGENTS.md

There is one per architectural layer.

#+begin_quote
Do not worship the current shape of the machine.  
Understand it.  
Then reshape it.  
Then watch the reshape begin to decompose.
#+end_quote

Remember: reality is a decomposing joke.
