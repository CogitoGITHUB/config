#+title:      modus-themes
#+identifier: 1d5b9e3f-7c2a-4e8d-af34-5b1c8e2d4f6a
#+filetags:   :emacs-package:ui:
#+PRIORITY: 1

* Modus-themes
:PROPERTIES:
:PACKAGE:  modus-themes
:STRAIGHT: (modus-themes :type git :host github :repo "protesilaos/modus-themes")
:CATEGORY: ui
:END:

** Default & Fringe

#+begin_src emacs-lisp
(defun manifold/pure-red--default ()
  (set-face-attribute 'default nil :foreground "#FFFFFF" :background "unspecified-bg")
  (set-face-attribute 'fringe nil :foreground "#FFFFFF" :background "unspecified-bg")
  (set-face-attribute 'region nil :foreground "#FFFFFF" :background "unspecified-bg")
  (set-face-attribute 'highlight nil :foreground "#FFFFFF" :background "unspecified-bg")
  (set-face-attribute 'cursor nil :background "#FFFFFF"))
#+end_src

** Focused Line
#+begin_src emacs-lisp
(defun manifold/pure-red--hl-line ()
  (with-eval-after-load 'hl-line
    (set-face-attribute 'hl-line nil :background "unspecified-bg" :inherit nil))
  (global-hl-line-mode 1))
#+end_src

** Org
#+begin_src emacs-lisp
(defun manifold/pure-red--org ()
  (dolist (level '(org-level-1 org-level-2 org-level-3
                   org-level-4 org-level-5 org-level-6 org-level-7 org-level-8))
    (set-face-attribute level nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'bold :inherit nil))
  (set-face-attribute 'org-document-title nil :foreground "#FFFFFF" :background 'unspecified :height 2.0 :weight 'bold :inherit nil :extend nil)
  (set-face-attribute 'org-level-1 nil :height 1.8)
  (set-face-attribute 'org-level-2 nil :height 1.6)
  (set-face-attribute 'org-level-3 nil :height 1.4)
  (set-face-attribute 'org-level-4 nil :height 1.2)
  (set-face-attribute 'org-link nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil)
  (dolist (face '(org-block org-block-begin-line org-block-end-line
                  org-code org-verbatim org-meta-line org-todo org-done
                  org-date org-special-keyword org-tag org-drawer
                  org-property-value org-table))
    (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil)))
#+end_src

** Code

#+begin_src emacs-lisp
(defun manifold/pure-red--code ()
  (dolist (face '(font-lock-comment-face font-lock-string-face font-lock-keyword-face
                  font-lock-function-name-face font-lock-variable-name-face
                  font-lock-type-face font-lock-constant-face font-lock-builtin-face
                  font-lock-warning-face font-lock-doc-face font-lock-preprocessor-face
                  font-lock-regexp-grouping-backslash font-lock-regexp-grouping-construct))
    (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil)))
#+end_src

** LaTeX

#+begin_src emacs-lisp
(defun manifold/pure-red--latex ()
  (with-eval-after-load 'tex-mode
    (dolist (face '(font-latex-math-face font-latex-script-char-face
                    font-latex-string-face font-latex-warning-face
                    font-latex-sedate-face font-latex-sectioning-0-face
                    font-latex-sectioning-1-face font-latex-sectioning-2-face
                    font-latex-sectioning-3-face font-latex-sectioning-4-face
                    font-latex-sectioning-5-face))
      (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :inherit nil))))
#+end_src

** PDF

#+begin_src emacs-lisp
(defun manifold/pure-red--pdf ()
  (with-eval-after-load 'pdf-view
    (setq pdf-view-midnight-colors '("#FFFFFF" . "unspecified-bg"))))
#+end_src

** Line Numbers

#+begin_src emacs-lisp
(defun manifold/pure-red--line-numbers ()
  (when (facep 'line-number)
    (set-face-attribute 'line-number nil :foreground "#FFFFFF" :background "unspecified-bg")
    (set-face-attribute 'line-number-current-line nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'bold)))
#+end_src

** Minibuffer

#+begin_src emacs-lisp
(defun manifold/pure-red--minibuffer ()
  (dolist (face '(minibuffer-prompt completions-annotations completions-common-part
                  completions-first-difference shadow help-key-binding))
    (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil))
  (when (facep 'completions-highlight)
    (set-face-attribute 'completions-highlight nil :foreground "#FFFFFF" :background "unspecified-bg" :inherit nil)))
#+end_src

** Vertico

#+begin_src emacs-lisp
(defun manifold/pure-red--vertico ()
  (with-eval-after-load 'vertico
    (dolist (face '(vertico-current vertico-group-title vertico-group-separator vertico-multiline))
      (when (facep face)
        (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :inherit nil)))))
#+end_src

** Orderless

#+begin_src emacs-lisp
(defun manifold/pure-red--orderless ()
  (with-eval-after-load 'orderless
    (dolist (face '(orderless-match-face-0 orderless-match-face-1
                    orderless-match-face-2 orderless-match-face-3))
      (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'bold :inherit nil))))
#+end_src

** Ivy

#+begin_src emacs-lisp
(defun manifold/pure-red--ivy ()
  (with-eval-after-load 'ivy
    (dolist (face '(ivy-current-match ivy-minibuffer-match-face-1 ivy-minibuffer-match-face-2
                    ivy-minibuffer-match-face-3 ivy-minibuffer-match-face-4 ivy-action
                    ivy-highlight-face ivy-remote ivy-subdir ivy-virtual ivy-confirm-face
                    ivy-match-required-face))
      (when (facep face)
        (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil)))))
#+end_src

** Helm

#+begin_src emacs-lisp
(defun manifold/pure-red--helm ()
  (with-eval-after-load 'helm
    (dolist (face '(helm-selection helm-match helm-source-header helm-visible-mark
                    helm-candidate-number helm-separator helm-M-x-key helm-buffer-directory
                    helm-buffer-file helm-buffer-process helm-ff-directory helm-ff-file
                    helm-grep-match))
      (when (facep face)
        (set-face-attribute face nil :foreground "#FFFFFF" :background "unspecified-bg" :weight 'normal :inherit nil)))))
#+end_src

** Mode Line

#+begin_src emacs-lisp
(defun manifold/pure-red--mode-line ()
  (set-face-attribute 'mode-line nil :foreground "#FFFFFF" :background "unspecified-bg" :box nil)
  (set-face-attribute 'mode-line-inactive nil :foreground "#FFFFFF" :background "unspecified-bg" :box nil)
  (set-face-attribute 'header-line nil :foreground "#FFFFFF" :background "unspecified-bg" :box nil :inherit nil))
#+end_src

** Apply

#+begin_src emacs-lisp
(defun manifold/pure-red ()
  (manifold/pure-red--default)
  (manifold/pure-red--hl-line)
  (manifold/pure-red--org)
  (manifold/pure-red--code)
  (manifold/pure-red--latex)
  (manifold/pure-red--pdf)
  (manifold/pure-red--line-numbers)
  (manifold/pure-red--minibuffer)
  (manifold/pure-red--vertico)
  (manifold/pure-red--orderless)
  (manifold/pure-red--ivy)
  (manifold/pure-red--helm)
  (manifold/pure-red--mode-line)
  )

(defun manifold/nuke-backgrounds ()
  (dolist (face (face-list))
    (when (and (not (memq face '(region cursor)))
               (not (string-match-p "avy-lead-face" (symbol-name face))))
      (set-face-attribute face nil :background 'unspecified))))

(manifold/pure-red)
(manifold/nuke-backgrounds)
(advice-add 'load-theme :after (lambda (&rest _) (manifold/pure-red) (manifold/nuke-backgrounds)))
(add-hook 'after-make-frame-functions (lambda (_frame) (manifold/pure-red) (manifold/nuke-backgrounds)))
#+end_src
