#+title:      dashboard (RETIRED — superseded)
#+filetags:   :emacs-package:ui:

* Dashboard — RETIRED

This module is superseded by:

: modules/manifolding-dashboard.org

Dashboard is now VENDORED there (upstream source embedded as src
blocks + all local configuration).  No ~:PACKAGE:/~:STRAIGHT:~ on
purpose — straight no longer owns it and must not re-install it.

Do not add a PACKAGE drawer back to this file.  Delete this file
once you are confident the vendored module is stable.
