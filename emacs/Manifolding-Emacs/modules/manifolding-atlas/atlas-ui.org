#+TITLE:      Manifolding Atlas UI
#+begin_comment
Sidebar, outline, backlinks, and UI configuration for manifolding-atlas.
#+end_comment
* Manifolding Atlas UI
:PROPERTIES:
:CATEGORY: ui
:END:
** Config
#+begin_src emacs-lisp
(unless (fboundp 'mode-line-invisible-mode)
  (defun mode-line-invisible-mode (&rest _) nil))
(with-eval-after-load 'manifolding-atlas
  (setq manifolding-atlas-ui-sidebar-position 'left
        manifolding-atlas-ui-sidebar-size     0.20)
  (setq manifolding-atlas-ui-outline-max-depth 3)
  (setq manifolding-atlas-ui-backlinks-show-preview t)
  (setq manifolding-atlas-ui-default-widget-collapsed nil)
  (setq manifolding-atlas-ui-fast-parse t)
  (setq manifolding-atlas-ui-auto-refresh t)
  (add-to-list 'warning-suppress-types '(org-element)))
#+end_src
