(define-module (substrate user-space root desktop video ffmpeg)
  #:use-module (gnu packages video)
  #:use-module (guix packages)
  #:re-export (ffmpeg))

;; Re-export of ffmpeg from gnu/packages/video
